<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>

        <!--
        Tests run against MySQL 8 — matches the production deployment on
        Laravel Cloud (`db:show` reports MySQL 8.0.39). Spin up the test
        DB via docker compose and Laravel will recreate the schema
        per-test class:

            docker compose exec mysql mysql -uroot -e "CREATE DATABASE crm_test;"
            docker compose exec mysql mysql -uroot -e "GRANT ALL ON crm_test.* TO 'crm'@'%';"

        CI uses identical settings (just override host/credentials).

        SQLite override: set DB_CONNECTION=sqlite + DB_DATABASE=:memory:
        in the running shell to run the unit suite without a DB server.
        Some feature tests (the HoldService MySQL GET_LOCK path, schema
        JSON casting) will skip or behave differently on sqlite.
        -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="3306"/>
        <env name="DB_DATABASE" value="crm_test"/>
        <env name="DB_USERNAME" value="crm"/>
        <env name="DB_PASSWORD" value=""/>

        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="FILESYSTEM_DISK" value="local"/>

        <!--
        Broadcasting in tests goes to the log driver. Domain events that
        implement ShouldBroadcast still dispatch listeners; the broadcast
        side just writes to laravel.log instead of hitting Pusher/Soketi.
        -->
        <env name="BROADCAST_CONNECTION" value="log"/>

        <!--
        Use array cache for tests so AgentPerformanceRepository's cache and
        the calling-window rule cache don't bleed across cases.
        -->
        <env name="CACHE_DRIVER" value="array"/>

        <!--
        Tests that exercise the dialer (PacingEngine, AgentPresenceService,
        LeadQueueService) need Redis. The local docker-compose stack provides
        it; CI brings up a redis service. We force the predis client so tests
        don't require the phpredis PHP extension. The dialer logical DB is
        15 to keep test data far away from any dev database.
        -->
        <env name="REDIS_CLIENT" value="predis"/>
        <env name="REDIS_HOST" value="127.0.0.1"/>
        <env name="REDIS_PORT" value="6379"/>
        <env name="REDIS_PASSWORD" value=""/>
        <env name="REDIS_DB" value="14"/>
        <env name="REDIS_CACHE_DB" value="14"/>
        <env name="REDIS_DIALER_DB" value="15"/>
        <env name="REDIS_BROADCAST_DB" value="14"/>
    </php>
</phpunit>
